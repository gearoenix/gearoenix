version: 2.1

orbs:
  codecov: codecov/codecov@1.0.4

jobs:
  build:
    docker:
      - image: 'noroozpour/gearoenix:0.0.1'
    steps:
      - checkout
      - run:
          name: Updating OS
          command: 'apt update && apt dist-upgrade -y && apt install lcov gcovr -y'
      - run:
          name: Setup dependancies
          command: 'python3 ./setup.py'
      - run:
          name: Cmake run
          command: 'cmake -DCMAKE_BUILD_TYPE=Debug .'
      - run:
          name: Build and tests run
          command: 'cmake --build . --config Debug --target GX_COVERAGE -- -j4'
      - codecov/upload:
          flags: unittest
          file: 'GX_COVERAGE.info'
          config: '.circleci/codecov.yml'
